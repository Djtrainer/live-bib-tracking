# Enough to import race_cv and the API and run tests/ -- no torch, no
# ultralytics, no EasyOCR. Tests that need the CoreML model or race footage
# skip themselves. See .github/workflows/tests.yml.
numpy>=2.0,<3
opencv-python-headless>=4.10,<5
PyYAML>=6.0
requests>=2.31
fastapi>=0.115,<1
starlette<1
uvicorn>=0.30
python-multipart>=0.0.9
httpx>=0.27          # FastAPI TestClient
pytest>=8.0
